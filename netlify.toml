[build]
  publish = "."

[[headers]]
  for = "/*"
    [headers.values]
    WWW-Authenticate = "Basic realm=\"Restricted Area\""
    Basic-Auth = ":${PASSWORD}"
    X-Debug-Info = "Netlify config processed"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = true